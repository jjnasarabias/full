apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: radius
spec:
  interval: 5m0s
  url: oci://ghcr.io/radius-project/helm-chart/radius
  layerSelector:
    mediaType: "application/vnd.cncf.helm.chart.content.v1.tar+gzip"
    operation: copy
  ref:
    semver: "^0.5.0"